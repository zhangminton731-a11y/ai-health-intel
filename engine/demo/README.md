# Demo output

Generated output is intentionally not committed. Run:

```bash
python3 scripts/run_demo.py --output-dir /tmp/sih-reference-demo
python3 -m http.server 8872 --directory /tmp/sih-reference-demo/site
```

The page must show the `SYNTHETIC DEMO · NO PERSONAL DATA` stamp.
